"""Read-only diagnostics over the extracted So2Sat patch data.

Scripts here MEASURE the existing pipeline; none of them modify data, models
or training behaviour. They are entry points, run from the repo root:

    python src/diagnostics/embedding_stats.py --help
    python src/diagnostics/verify_alphaearth_dequant.py --help
"""
